<%- [@listeners].flatten.compact.each do |listener| -%>
  <%- listener.each_pair do |attrib, value| %>
    <%- if attrib == listener.keys.first -%>
    <Listener <%= attrib %>=<%= "#{value}".encode(:xml => :attr) -%>
    <%- else -%>
              <%= attrib %>=<%= "#{value}".encode(:xml => :attr) -%>
    <%- end -%>
  <%- end %> />
<%- end -%>
