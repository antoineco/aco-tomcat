fixtures:
  forge_modules:
    concat:
      repo: puppetlabs/concat
      ref: 2.2.1
    firewall: puppetlabs/firewall
    archive: puppet/archive
    stdlib: puppetlabs/stdlib
  symlinks:
    tomcat: "#{source_dir}"
